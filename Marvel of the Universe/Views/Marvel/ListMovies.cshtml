@model IEnumerable<Marvel_of_the_Universe.Models.Movie>
<div class="row">
    @foreach (var item in Model)
    {
        <div class="card-group col-md-4" style="padding:10px;">
            <div class="card text-danger border-danger ">
                <a href="@Url.Action("Movie","Marvel", new { id = item.Id})"><img height="350" width="140"  src="~/Images/Movies/@item.Image" class="card-img-top figure-img" /></a>
                <h5 class="card-title text-center">@Html.DisplayFor(modelItem => item.Name)</h5>
            </div>
        </div>
    }
</div>
